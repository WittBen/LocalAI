<Window x:Class="LocalChatBot.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LocalChatBot"
        mc:Ignorable="d"
        Title="Foundry Chat" Height="550" Width="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <Label Content="Enter your message:"/>
            <TextBox x:Name="InputTextBox" Margin="0,0,0,10" Height="60" TextWrapping="Wrap" AcceptsReturn="True"/>
            <Button x:Name="SubmitButton" Content="Send to Model" Click="SubmitButton_Click" Width="200" HorizontalAlignment="Left"/>
        </StackPanel>

        <ScrollViewer x:Name="ChatScrollViewer" Grid.Row="1" Margin="0,10,0,0" VerticalScrollBarVisibility="Auto">
            <StackPanel x:Name="ConversationStackPanel"/>
        </ScrollViewer>

        <StatusBar Grid.Row="2">
            <StatusBarItem>
                <TextBlock x:Name="StatusTextBlock" Text="Ready."/>
            </StatusBarItem>
        </StatusBar>
    </Grid>

</Window>
